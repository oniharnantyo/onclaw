# add-agent-streaming

Enable token-level streaming on the web channel (live typing) via Eino's per-call EnableStreaming flag, gated per channel; CLI stays message-granular. Amends agent-core streaming requirement; adds chat-ui token-delta rendering.
