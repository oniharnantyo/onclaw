# add-onboarding-cli

Guided first-run onboarding (`onclaw init`) plus a reusable `onclaw provider setup` step that configure one or more LLM providers and a default interactively, layered over the existing provider service with no new dependencies.